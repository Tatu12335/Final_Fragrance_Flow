<Window x:Class="Fragrance_Flow_WPF_.Loginwindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Fragrance_Flow_WPF_"
        mc:Ignorable="d"
        Title="Loginwindow" Height="450" Width="800">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <StackPanel x:Name="stackPanel" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Background="Beige">
            <TextBlock Text="Username:" Margin="0,0,0,5"/>
            <TextBox x:Name="UsernameTextBox" Width="200" Margin="0,0,0,10" Background="AliceBlue"
                     BorderBrush="Silver" BorderThickness="3px"/>
            <TextBlock Text="Password:" Margin="0,0,0,5"/>
            <PasswordBox x:Name="PasswordBox" Width="200" Margin="0,0,0,20" Background="AliceBlue"
                         BorderBrush="Silver" BorderThickness="3px"/>
            <Button Content="Login" Width="100" Click="LoginButton_Click" BorderBrush="Silver" BorderThickness="3px" RenderTransformOrigin="0.5,0.5" Cursor="Hand">

            </Button>
        </StackPanel>
        <Label x:Name="Welcome" Content="Welcome to Fragrance Flow!" FontSize="24" FontWeight="Bold" Foreground="DarkBlue" Grid.Column="1" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">
        </Label>
        <TextBlock Grid.Row="2" Grid.Column="1" Margin="17,19,222,77" RenderTransformOrigin="0.501,-0.307">
            <Hyperlink Click="Hyperlink_Click">
                forgot password?
            </Hyperlink>
        </TextBlock>
        <TextBlock Grid.Row="2" Grid.Column="1" Margin="220,20,34,75" RenderTransformOrigin="0.501,-0.307">
    <Hyperlink Click="Hyperlink_Click_1">
        create account
    </Hyperlink>
        </TextBlock>

        <Label x:Name="ErrorMessage" Content="" Foreground="Red"  Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,75,0,0" Width="572" Grid.Column="1"/>


    </Grid>
</Window>
